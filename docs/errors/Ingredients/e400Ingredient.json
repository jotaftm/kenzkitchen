{
  "required": ["status", "message"],
  "properties": {
    "status": {
      "type": "string",
      "example": "error"
    },
    "message": {
      "type": "array",
      "example": [
        "email is required",
        "quantity is required",
        "price is required"
      ]
    }
  }
}
