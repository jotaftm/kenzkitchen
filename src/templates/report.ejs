<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      h1 {
        margin: 5px 0;
        text-align: end;
        font-size: 24px;
      }
      /* div {
        border: 1px solid black;
        border-bottom: none;
      } */
      h2 {
        /* text-align: center; */
        margin-bottom: 0.2rem;
        font-size: 20px;
      }
      p {
        margin: 0;
        padding: 5px 0;
        /* border-bottom: 1px solid black; */
        padding: 0.2rem;
        font-size: 10px;
      }
      table {
        min-width: 250px;
        text-align: center;
        border: 1px solid black;
        border-radius: 5px;
        margin: 0 auto;
        border-collapse: collapse;
        font-size: 12px;
      }
      td {
        width: 140px;
        border: 1px solid black;
      }
    </style>
  </head>
  <body>
    <h1>KenzKitchen</h1>
    <div>
      <p>Empresa: <%= company %></p>
      <p>CNPJ: <%= cnpj %></p>
      <p>Data de solicitação: <%= date %></p>
      <p>Data de execução: <%= scheduled %></p>
      <p>Emitido por: <%= user %></p>
    </div>
    <h2>Ordem de serviço</h2>
    <div>
      <p>Identificação: <%= orderId %></p>
    </div>
    <table>
      <thead>
        <tr>
          <th>Ingrediente</th>
          <th>Quantidade necessária</th>
          <th>Estoque</th>
          <th>Reposição</th>
        </tr>
      </thead>
      <% ordersIngredients.map((orderIngredient) => { %>
      <tr>
        <td><%= orderIngredient.ingredient.name %></td>
        <td>
          <%= orderIngredient.quantity %> <%= orderIngredient.ingredient.unity
          %>
        </td>
        <td>
          <%= orderIngredient.ingredient.quantity %> <%=
          orderIngredient.ingredient.unity %>
        </td>
        <% if (orderIngredient.ingredient.quantity > orderIngredient.quantity) {
        %>
        <td><%= 0 %></td>
        <% } else { %>
        <td>
          <%= orderIngredient.quantity - orderIngredient.ingredient.quantity %>
          <%= orderIngredient.ingredient.unity %>
        </td>
        <% } %>
      </tr>
      <% }) %>
    </table>
  </body>
</html>
